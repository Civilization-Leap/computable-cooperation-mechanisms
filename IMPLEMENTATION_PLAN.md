# 竞争合作可计算机制｜最小开源参考实现
# 开发实施计划 V0.1

**编制日期：2026-09-06**  
**状态：建议执行稿；尚未冻结、尚未构成发布授权。**  
**研究作者：子君赋｜出品：文明跃迁研究组**

本件只形成软件开发实施计划，不表示代码、自动测试、独立复核、远端仓库或开源发布已经完成。软件版本与 MASTER/V1.0、WP-01-A、FNX 研究文件版本及 R0—R6 研究成熟度分别登记，不互相替代。

## 1. 首期目标与结束条件

以最小个人精力和资源投入，面向全球交付一个能够运行、理解、复算和继续开发的开源起点。首期建设对象是小型研究参考实现，不是由发起人长期运营的现实谈判平台、公共服务或商业产品。

**首期结果：一套有限计算内核＋一个完整公开教学场景族＋自动测试＋中英入口与接续说明＋明确许可。**

一个未参加前期讨论的开发者，能够仅凭仓库说明完成运行、复算、改变条件，并在不修改核心计算流程的情况下添加一个同类场景，即达到技术接续目标。没有招到贡献者、没有商业收入，不构成首期未完成。

## 2. 研究基线与材料边界

研究方向与纪律以《竞争—合作—联盟—阵营—共同体可计算机制研究》MASTER/V1.0 FROZEN 为参照，不退回 V0.2、V0.3 或 V0.4 候选稿，不通过工程便利改写冻结命题、术语或权限。[S1]

本计划是软件工程伴随件，不自称已取得 MASTER 的正式附件派生资格或全协议兼容认证。公平谈判研究线与 WP-01-A 保持相互独立，共享技术构件不继承研究状态、证据或结论。[S2]

现有 R1 B2B 夹具属于控制母本，包含参考答案，尚不等于已封存、已分发或已激活的运行包。本首版不复制、加载、执行、改名复用或发布其中的目标场景及答案；也不开发其激活令牌、盲复核、评分或现实角色系统。[S3]

公开教学场景另行原创，使用独立标识、数值与规则。它公开全部软件测试预期，不主张盲法、独立研究证据、正式 R1 结果或现实机制有效。涉及正式研究的后续实现，须另按该研究线的现行范围处理。

## 3. 首期范围

| 组成 | 必须交付 | 明确不做 |
|---|---|---|
| 项目入口 | 英文主 README 与对应中文说明；目的、目标、方法、范围、使用方式、局限、贡献与商用关系 | 全部研究母稿翻译、独立官方网站、多语言运营 |
| 数据合同 | 主体、基线、候选方案、利益与负担维度、当前资源、时间窗、第三方、约束、来源和未知 | 真人画像、人格或合作度评分、默认采集真实资料 |
| 有限计算内核 | 数值条件检查、分项差异、硬约束检查、未知保留、同场景条件变体比较 | 自动决定公平、通用最优方案搜索、主体行为预测 |
| 公开示例 | 一个完整合成场景族，至少包含满足约束、违反约束、信息不足三种变体 | 正式研究夹具、真实谈判、真实服务控制 |
| 结果与复算 | JSON 结果与可阅读的 Markdown 报告；输入/规则/软件版本和计算依据 | 汇总公平分、现实 ALLOW/BLOCK 指令、认证证书 |
| 接续保障 | 自动测试、同类场景扩展示例、简短贡献说明、版本记录与许可范围 | 插件市场、商业审批系统、长期维护承诺 |

不把未实现项自动登记为发起人的待偿债务。下游团队可以独立研究这些方向。

## 4. 最小技术方案

建议使用 **Python＋JSON＋本地命令行＋标准库优先的自动测试**。Python 官方文档提供 JSON 编解码、十进制计算及 unittest 测试能力；具体兼容版本必须以实现时的实际测试为准，不宣称未测试的平台或版本已支持。[W1–W3]

目标是不依赖付费模型、云服务、数据库、账户系统或常驻后台。运行不发起外部业务请求、不采集遥测；首次准备运行环境所需下载，与系统运行时的联网依赖分开说明。

采用单一代码库，数据与规则、计算与展示、示例与内核分别存放。通过小型明确函数接口支持替换，不预建复杂插件框架。

建议目录仅用于工程定位，不是已确认的全球项目名称：

```text
README.md
README.zh-CN.md
IMPLEMENTATION_PLAN.md
pyproject.toml
mechanism_ref/       # 数据读取、验证、有限计算、报告、命令行
examples/           # 原创公开教学输入与预期解释
schemas/            # 本软件版本的数据格式定义
tests/             # 自动测试
docs/              # 方法边界、来源对应与同类场景扩展说明
LICENSE             # 发布前确认后写入正式文本
CONTRIBUTING.md
CHANGELOG.md
```

代码支持范围与数据格式不得冒充现有 WP-01-A 或 FNX 合同的完整实现；这些协议的序列化规则也不应被强行合并。

### 4.1 计算内核的确切职责

第一，检查输入结构、引用、单位、数值范围和时间窗。不支持的字段、重复键、非有限数值、异常规模与不可解析数据，应明确拒绝，不能静默修正成有利输入。资源限制与重复键处理必须在实现中明示；不能只依赖 JSON 默认解析行为。[W1]

第二，在同一主体、同一维度和可比较时间窗内计算基线与候选方案的差异。不同维度、不同单位、不相容时间窗不得直接加总。感知利益、输入假设和后果记录分别保留。

第三，根据显式规则检查资源用量、时间可用性、第三方负担和硬约束。未来可能形成的能力不能被自动算作当前资源；增加普通收益不能抵销另一条硬约束。

第四，对合法输入中明确未知的关键值输出无法判定及其原因。数据不合法与数据合法但未知是两类不同情况。未输入的第三方不自动等于不存在第三方。

第五，在完全相同的输入、规则和软件版本下重复计算，产生一致的核心结果。若报告有生成时间或运行路径等非语义信息，必须与确定性结果分开。

### 4.2 输出身份

每项程序检查只报告“满足已声明约束／违反已声明约束／信息不足无法判定”及其字段、规则与计算依据。禁止据此自动形成 H、T、L、RUN、现实授权或公平认证状态。

首版计算的是场景中明确给定的条件、候选安排与假设，不自行发现真实效用、因果参数、主体意图或社会未来。输入假设的可复算后果，不是现实有效性的证明。[S1–S2]

输入及结果保留版本与内容校验标识，只用于定位和复算；哈希本身不证明事实真实、独立性或不可篡改。程序不覆盖输入原件；修订应另存并保留来源关系。

## 5. 首个公开教学场景

建议采用 **纯合成的共享研究设备时段与准备成本分配**：两个主要协作主体，以及一个可能被挤占保留时段的第三方。

在给定资源、时间、准备成本和保留规则下，比较分散使用的基线与一个协作安排。所有数值、利益代理、保护约束及其选择理由公开，并明确只在该教学模型内有效。

| 变体 | 软件需展示的区别 |
|---|---|
| 条件足够 | 按主体与维度呈现变化，逐项显示已声明约束是否满足；不标“公平已实现” |
| 第三方负担超界 | 即使两个主要主体的普通成本下降，第三方保护约束仍单列为违反 |
| 关键资源未知 | 缺少行动时点的可用资源信息，相关检查保持无法判定，不补零或默认乐观值 |

不把这个设备协作示例叫作 FNX-R1、WP-01-A 或其复制研究。增加第二份同类输入只验证代码可扩展，不宣称跨领域机制有效。

## 6. 开发实施顺序

以下 DEV 编号只表示软件交付批次，不是研究成熟度，也不是四次逐项审批。

| 批次 | 工作 | 实际交付 | 完成依据 |
|---|---|---|---|
| DEV-1｜最小可运行闭环 | 同步写简明规格、数据合同、核心计算、首个示例与基础测试 | 本地代码、输入、输出报告、运行说明 | 在干净目录按说明运行；至少一条实际计算从输入贯通到报告，不是占位函数 |
| DEV-2｜失败、未知与复算 | 补齐三种变体、边界检查、确定性复算与同类输入扩展 | 自动测试及真实执行记录、修复后的代码 | 关键测试全部通过；缺陷和未测范围明示；不以调低标准换通过 |
| DEV-3｜全球开源候选包 | 完成中英入口、方法与边界、来源对应、许可候选清单、贡献和维护说明 | 仓库就绪候选包、发布检查清单 | 代码/文档/示例一致；材料可公开范围明确；未授权资料不进入发布包 |
| DEV-4｜首次公开发布 | 确认名称、仓库归属、许可覆盖与公开范围；执行并核验发布 | 实际仓库、首个版本与可访问发布记录 | 只有远端实际公开且内容与候选包一致，才登记“已发布” |

每批更新同一份实施记录：实际产物、版本或提交、测试结果、未关闭问题、下一项工作。内部提交不需要生成一系列独立 Word 母稿。

DEV-3 可以在许可待确认时完成内部候选包，但不得宣称它已经正式开源。DEV-4 的公开动作须另获明确授权；开发授权不等于公开资料的授权。

## 7. 首版验收

| 检查组 | 必需结果 |
|---|---|
| 能运行 | 说明中的命令真实有效；不要求付费模型密钥；已测试环境明确 |
| 有实质计算 | 改变一个资源或时间条件会经算法改变相应检查结果，不靠场景名返回预写答案 |
| 不抵销硬边界 | 双边普通收益提高不能消除第三方硬约束违反；每项结果保持可见 |
| 不吞掉未知 | 未知不填零，不等于无影响；未来能力不自动冒充当前能力 |
| 错误可辨 | 单位错配、非法引用、无效版本、非有限值等清楚拒绝，并区别于信息不足 |
| 能复算 | 固定输入、规则与版本得到一致核心结果；原输入不被覆盖 |
| 能扩展 | 新增一份同类场景输入不需改核心流程；替换规则的最小接口有示例 |
| 不越界 | 测试通过不生成机制、公平或现实授权结论；无真实外部业务动作 |
| 可全球接续 | 英文和中文的功能范围、命令、限制与许可表述一致 |
| 可公开 | 发布树及将公开的历史中无受限夹具、答案、私人材料、凭证或未经许可内容 |

可先完成本地自动化测试；只有远端实际执行后，才登记远端持续集成通过。不同模型或会话的重复检查，不被标成独立研究复核。

## 8. 精力、费用与维护控制

实施端承担规格转代码、测试、修复与文档；发起人不承担日常环境调试、逐个技术方案选型或用户支持。首期方向已经在本对话明确，实施中不得反复要求重做同一定位裁决。

公开发布前，集中确认项目中英文名称与仓库归属、许可证与覆盖范围、首版公开内容。新增支出、真实数据使用或首期范围扩张须另行授权。

**资源边界：一个代码库、一个有限内核、一个教学场景族；不采购服务器，不引入付费模型依赖，不建设常驻服务。**这不等于声称已有设备、账户套餐或开发劳动没有成本。

工期先按验收节点管理，不编造未经实现验证的总天数。DEV-1 后记录实际实现与返工情况，才估计余下工作。遇到超出投入目标的需求，优先移出界面、自动优化、多智能体和跨域功能，而不是削弱数据、计算与测试正确性。

首发完成且可独立接续，即结束首期主动建设。后续功能列为开放议题而非作者欠账。不主动承诺更新频率、响应期限、定制支持或服务可用性。已知会造成误导或披露问题的缺陷，应如实标记，并视情况修复或撤下受影响版本，不能用“研究软件”掩盖已知错误。

## 9. 开源许可与独立商用

代码许可候选仍为 **Apache-2.0，尚未确认或生效**。发布前需明确代码、随附工程文档和原创合成示例各自的许可覆盖，避免只有代码可复用而示例和说明无法合法接续。原研究母稿、受限资料和品牌名称不默认随代码开放。[W4–W6]

若最终采用 Apache-2.0，商业团队可按其条款使用、修改和分发，亦可能形成闭源产品；须履行适用的许可、版权与通知保留等义务。许可证不自动授予项目商标或官方背书。[W5–W6]

建议商业产品采用独立主体、仓库、品牌、数据和服务责任。其销售、实施、托管、支持或行业验证不成为公共参考实现的首发前置任务。发起人可以参与，也可以不参与；本计划不成立公司、不签商业合同、不授权品牌使用。

## 10. 当前完成状态与唯一接力项

**本轮完成：开发实施计划建议执行稿 V0.1。**  
**未完成或未核验：本项目代码、测试执行、远端仓库、持续集成、许可生效与公开发布。**  
**尚待发布前裁决：正式中英文名称、仓库归属、许可证覆盖和首版公开内容。**

**下一项：DEV-1——交付“数据合同＋原创公开示例＋有限计算内核＋报告＋基础测试”的第一个可运行闭环。**不再先写第二份大型架构论证；不执行正式研究夹具或现实操作。

---

## 来源与采纳边界

以下材料支撑研究约束或技术事实；批次、目录、教学场景、资源上限和实施组织是本计划新增的工程建议，不冒充母本已有条款。

- **[S1]** 《竞争合作联盟阵营共同体_可计算机制研究_MASTER_V1.0_FROZEN.docx》：项目身份、可计算含义、主张与工具边界、母本—附件关系。本轮通过文件库检索核对，未将全文复制进本计划。
- **[S2]** 《多主体公平谈判与利益平衡机制研究_V0.1_跨领域扩展立项基线.docx》：第零节的独立相邻研究线关系；第四至七节的主张、能力与双轨；第十一节的方法证明上限；第十六节接口。
- **[S3]** 《多主体公平谈判与利益平衡机制研究_R1纯合成B2B服务采购履约变更_场景角色扰动与盲复核夹具包_V0.1.docx》：首页及第零、一节的控制母本身份、未激活、未物理封存与禁止派发边界。
- **[W1]** Python 官方文档：json — JSON encoder and decoder；核验日期 2026-09-06。引用仅限 JSON 输入输出能力、默认非有限值/重复键行为与资源限制提示。
- **[W2]** Python 官方文档：unittest — Unit testing framework；核验日期 2026-09-06。
- **[W3]** Python 官方文档：decimal — Decimal fixed-point and floating-point arithmetic；核验日期 2026-09-06。使用时仍须显式确定精度、单位和舍入规则。
- **[W4]** GitHub 官方文档：Licensing a repository；核验日期 2026-09-06。仓库公开与明确许可是不同事项。
- **[W5]** Apache Software Foundation：Apache License, Version 2.0，尤其第 2、3、4、6、7—9 节；核验日期 2026-09-06。
- **[W6]** Apache Software Foundation：Apache Licensing and Distribution FAQ；核验日期 2026-09-06。许可摘要不替代许可证原文或具体法域下的专业法律意见。
