# 项目总览

## 项目目的

本项目研究：如何把多主体之间的竞争、合作、联盟、阵营与共同体关系，转化为能够被明确表达、检查、复算、质疑和继续开发的机制对象，而不是只停留在叙事判断或黑箱结论中。

当前目标刻意保持小而清楚：提供一个最小开源参考实现，降低研究者、开发者、机构与独立团队测试、反驳、替换和扩展机制设计的成本。

本项目**不预设合作总是更优，不把达成协议等同公平，也不把可计算结果自动等同正当结果**。失败、冲突、硬约束、第三方负担和信息不足，都是正式结果，而不是需要被隐藏的异常。

## 核心研究问题

当不同主体拥有不同利益、能力、信息、时间条件、依赖关系与退出选项时，改变互动规则之后究竟发生什么？谁改善、谁承担代价、哪些硬边界被跨越、哪些事实仍未知，以及所谓“改进”会在什么条件下失效？

## 当前目标

最小参考实现只把其中一部分问题转成机器可检查对象：

- 表达主体、现实基线、候选安排、结果、资源与显式约束；
- 只比较可比的主体／维度／单位，不生成普遍“公平总分”；
- 把普通收益与硬约束、第三方保护分开；
- 明确保留 `UNKNOWN`，不把缺失事实静默补零或乐观推定；
- 区分输入非法与信息合法但未知；
- 输出确定、可检查、可独立复算的结果；
- 为后续新增场景和机制模块保留最小扩展路径。

## 方法

当前软件遵守五条方法纪律。

1. **先声明，再计算。** 程序只处理输入中明确给定的主体、数值、单位、资源和约束，不自行推断隐藏动机、真实偏好、法律权利或社会事实。
2. **维度分开。** 只有主体、维度和单位一致时才进行结果比较；不生成普遍公平、福利、合作或文明等级分数。
3. **硬约束不能被平均值吞掉。** 普通收益不能自动抵销已经违反的硬约束或第三方保护边界。
4. **未知是合法结果。** 合法输入中的关键值明确未知时输出 `UNKNOWN`；非法引用、未声明单位等则直接拒绝，不能伪装成未知。
5. **复算是结果的一部分。** 同一合法输入与同一软件版本应得到一致核心结果，并通过输入哈希支持复算定位。

## 应用方式

当前参考实现适用于：

- 教学与演示；
- 纯合成机制实验；
- 可复现研究原型；
- 多方独立二次开发；
- 显式规则集之间的比较；
- 由领域团队自行承担验证和治理责任的专业工具开发。

典型路径为：

`描述场景 → 声明基线与候选安排 → 声明约束 → 运行评估器 → 查看 SATISFIED / VIOLATED / UNKNOWN → 修改条件 → 重新计算`

## 范围

### 当前软件已经覆盖

当前候选版本支持本地 Python/JSON 工作流、合成场景、确定性比较、硬约束检查、明确未知、机器可读 JSON 输出、人类可读 Markdown 报告和自动测试。

### 长期研究方向，不是当前软件已经证明的能力

更大的研究计划未来可以继续研究谈判、供应链、劳动关系、平台治理、公共项目、联盟、阵营、共同体、公共品、能力发展、外部性、纠错与退出等多主体机制。

这些领域不共享一个万能制度。一个合成示例成功运行，不能证明现实机制有效，更不能自动外推至其他领域。

### 当前明确不做

本仓库不提供：

- 公平认证；
- 法律、政策、投资、就业、谈判或公共权力决策；
- 行为或动机预测；
- 人格、忠诚、合作度、意识形态或信誉评分；
- 自动最优政策搜索；
- 统一全球谈判或治理制度；
- 现实授权、执行、仲裁或代表权；
- 对输入事实真实、完整、正当或伦理可接受的保证。

正式研究母稿、受控盲测夹具、受限研究资料、个人数据以及其他未进入本仓库的资产，不因本仓库开源而自动纳入许可范围。

## 商业与独立二次开发

Apache-2.0 允许依许可证条款进行商业使用。独立团队可以开发产品、服务、私有扩展、研究工具、托管系统、集成方案或领域专版。

公共研究项目与任何下游商业实现责任独立。下游团队应当自行承担其宣传、数据、验证、安全、合规、部署、客户支持和现实后果责任。

使用本仓库不意味着获得项目背书、公平认证、下游机制有效性认定，也不意味着取得代表任何个人或机构行动的授权。

## 全球参与方式

项目设计目标是“可分叉、可接续”，而不是要求所有后续开发都依赖中心维护者。贡献者不需要先接受全部理论命题，才可以测试、批评或扩展软件。

有价值的贡献包括：

- 反例与失败场景；
- 更严格的输入校验；
- 更清晰的数据结构和文档；
- 复算与可重复性改进；
- 新的原创合成示例；
- 替代机制模块；
- 清楚限定适用范围的领域实现；
- 翻译与可访问性改进。

主仓库维护者可以决定是否合并贡献，但在 Apache-2.0 允许的范围内，下游独立开发不需要获得主仓库逐项审批。

## 维护责任边界

本项目是开放研究起点，不是持续托管服务承诺，也不默认承担无限技术支持。除非某个下游服务主体另行作出承诺，本项目不保证在线可用性、响应时间、路线图、长期兼容或定制开发。

## 许可证

除非具体文件另有说明，本仓库原创代码、文档、合成教学示例、测试和项目支持文件统一采用 Apache License, Version 2.0。详见 [`../LICENSE`](../LICENSE) 与 [`../LICENSE_SCOPE.md`](../LICENSE_SCOPE.md)。
